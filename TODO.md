# TODO

- Split more of `pdb_to_excel.py` into focused modules
- Add a thin `cli.py` entry if the CLI and workbook-generation code are separated cleanly
- Expand test coverage around workbook generation paths that use offensive and defensive game plans together
